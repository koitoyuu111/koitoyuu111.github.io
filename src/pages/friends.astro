---
// 友链页临时下线（302）：
// 原因：src/content/friends/ 暂无友链数据，页面渲染为空，且构建时产生
// "The collection 'friends' does not exist or is empty" 警告。
// 恢复方法：git 中找回本文件原内容（含 getCollection("friends") 逻辑），
// 在 src/content/friends/ 下添加友链 JSON 文件后重新部署即可。
return Astro.redirect("/", 302);
---
